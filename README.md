## Custom bash scripts, aliases, themes, and functions

To customize bash-it set `BASH_IT_CUSTOM` in your `~/.bashrc` to the location where this directory is cloned outside of the `~/.bash_it` folder.
